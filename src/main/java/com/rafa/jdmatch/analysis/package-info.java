/**
 * Gap-analysis pipeline: orchestrates a JD and a resume into a typed analysis.
 * Owns the {@code POST /analyses} and {@code GET /analyses/{id}} endpoints.
 * Skeleton only — no logic yet.
 */
package com.rafa.jdmatch.analysis;
