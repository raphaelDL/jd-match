# Copy to terraform.tfvars and fill in. Do NOT commit the real file (gitignored).
region            = "us-east-1"
db_password       = "change-me-strong-password"
anthropic_api_key = "sk-ant-..."
# image_tag       = "latest"   # bump after pushing a new image
